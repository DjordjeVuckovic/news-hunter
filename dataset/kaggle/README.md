# Kaggle datasets

- Make sure to execute scripts/install_kaggle_*.sh to install Kaggle API and set up authentication.
- After setting everything up, datasets will be downloaded to the `dataset/kaggle/` directory.